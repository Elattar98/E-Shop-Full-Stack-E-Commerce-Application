{{# def.definitions }}
{{# def.errors }}
{{# def.setupKeyword }}

{{
  var $key = 'key' + $lvl
    , $idx = 'idx' + $lvl
    , $matched = 'patternMatched' + $lvl
    , $dataProperties = 'dataProperties' + $lvl
    , $closingBraces = ''
    , $ownProperties 